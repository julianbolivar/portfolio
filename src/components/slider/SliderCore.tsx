import styled from "styled-components";

const NavigationWrapper = styled.div`
  position: relative;
  overflow: hidden;
  margin-bottom: 1.2rem;
`;

const KeenSlider = styled.div`
  overflow: visible;
`;

export { NavigationWrapper, KeenSlider };
